<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>kanty - Neon Tea Experience</title>
  <link href="https://fonts.googleapis.com/css2?family=Rubik+Spray+Paint&display=swap" rel="stylesheet">
  <style>
    :root {
      --green: #39ff14;
      --pink: #ff1fd6;
      --black: #111;
      --white: #fff;
    }
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      background: var(--black);
      color: var(--green);
      font-family: 'Rubik Spray Paint', cursive, sans-serif;
      display: flex;
      flex-direction: column;
    }
    header {
      background: var(--pink);
      color: var(--black);
      padding: 2rem 0 1rem 0;
      text-align: center;
      letter-spacing: 2px;
      box-shadow: 0 4px 24px var(--pink);
    }
    header h1 {
      margin: 0;
      font-size: 3rem;
      text-shadow: 0 0 8px var(--green), 0 0 20px var(--pink);
    }
    nav {
      margin-top: 1rem;
    }
    nav a {
      color: var(--green);
      background: var(--black);
      border: 2px solid var(--green);
      padding: 0.5rem 1.2rem;
      margin: 0 0.5rem;
      border-radius: 20px;
      text-decoration: none;
      font-size: 1.1rem;
      transition: background 0.2s, color 0.2s, box-shadow 0.2s;
      box-shadow: 0 0 8px var(--green);
    }
    nav a:hover {
      background: var(--green);
      color: var(--black);
      box-shadow: 0 0 16px var(--pink);
    }
    .hero {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 3rem 2rem;
    }
    .hero-title {
      font-size: 2.5rem;
      color: var(--green);
      text-shadow: 0 0 14px var(--pink);
      margin-bottom: 1rem;
    }
    .hero-desc {
      color: var(--white);
      background: rgba(255, 31, 214, 0.1);
      padding: 1.2rem 2rem;
      border-radius: 12px;
      font-size: 1.2rem;
      max-width: 600px;
      box-shadow: 0 0 20px var(--pink);
      margin-bottom: 2rem;
    }
    .cta-btn {
      background: var(--green);
      color: var(--black);
      border: none;
      padding: 1rem 2.5rem;
      border-radius: 25px;
      font-size: 1.3rem;
      font-family: 'Rubik Spray Paint', cursive;
      box-shadow: 0 0 18px var(--pink);
      cursor: pointer;
      transition: background 0.2s, color 0.2s, box-shadow 0.2s, transform 0.1s;
    }
    .cta-btn:hover {
      background: var(--pink);
      color: var(--white);
      box-shadow: 0 0 32px var(--green);
      transform: scale(1.05);
    }
    .flavors {
      margin: 3rem auto 2rem auto;
      background: #1a1a1a;
      border-radius: 18px;
      max-width: 800px;
      box-shadow: 0 0 16px var(--green);
      padding: 2rem;
      text-align: center;
    }
    .flavors h2 {
      color: var(--pink);
      margin-bottom: 1.5rem;
      text-shadow: 0 0 10px var(--green);
    }
    .flavor-list {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.2rem;
      padding: 0;
      list-style: none;
    }
    .flavor-list li {
      background: var(--black);
      border: 2px solid var(--green);
      border-radius: 12px;
      color: var(--white);
      min-width: 140px;
      padding: 1rem 1.2rem;
      font-size: 1.1rem;
      box-shadow: 0 0 10px var(--green);
      transition: border 0.2s, color 0.2s;
    }
    .flavor-list li:hover {
      border: 2px solid var(--pink);
      color: var(--pink);
    }
    footer {
      background: var(--pink);
      color: var(--black);
      text-align: center;
      padding: 1rem 0 0.5rem 0;
      margin-top: 3rem;
      font-size: 1.1rem;
      box-shadow: 0 -1px 12px var(--green);
    }
    @media (max-width: 700px) {
      .hero-title { font-size: 2rem; }
      .flavors { padding: 1rem; }
    }
    @media (max-width: 500px) {
      header h1 { font-size: 2rem; }
      .hero-title { font-size: 1.3rem; }
      .hero-desc { padding: 0.7rem 0.5rem; }
      .flavor-list li { font-size: 1rem; min-width: 96px; padding: 0.7rem 0.5rem; }
    }
  </style>
</head>
<body>
  <header>
    <h1>kanty</h1>
    <nav>
      <a href="#flavors">Flavors</a>
      <a href="#">Order</a>
      <a href="#">Reviews</a>
      <a href="#">Contact</a>
    </nav>
  </header>
  <main>
    <section class="hero">
      <div class="hero-title">Neon-Infused Tea for a New Generation</div>
      <div class="hero-desc">
        Bold, bright, and never boring—kanty crafts tea that glows with flavor and attitude. 
        Try our signature blends, bursting with neon vibes and crafted for those who want their tea as unique as they are.
      </div>
      <button class="cta-btn">Order Now</button>
    </section>
    <section class="flavors" id="flavors">
      <h2>Our Flavors</h2>
      <ul class="flavor-list">
        <li>Fresh &amp; Clean (Green Tea)</li>
        <li>Bold &amp; Smooth (Black Tea)</li>
        <li>Sun Drip (Lemon Honey Black)</li>
        <li>Peach Fade (Peach Green Tea)</li>
        <li>Crimson Rush (Hibiscus Berry)</li>
        <li>Mango Voltage (Mango Citrus)</li>
        <li>Street Sweet (Sweet Black Tea)</li>
      </ul>
    </section>
  </main>
  <footer>
    &copy; 2025 kanty | Tea with neon attitude | Follow us @kanttea
  </footer>
</body>
</html>
